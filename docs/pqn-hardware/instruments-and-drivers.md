# Instruments & drivers

> _Placeholder — to be drafted in Phase 3._ This page will cover the `Instrument`
> `typing.Protocol` (`parameters` vs `operations`, `InstrumentInfo`, the
> `log_operation` / `log_parameter` decorators, the typed sub-protocols), writing a
> Driver (worked example on `DummyInstrument` plus a tour of the real drivers), and
> the ProxyInstrument lifecycle.
