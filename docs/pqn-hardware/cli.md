# The `pqn-hw` CLI

> _Placeholder — to be drafted in Phase 5._ This page will document `start-router`
> and `start-provider`, the TOML config shape and every flag, with the corrected
> `config_example.toml` lifted as the worked example.
